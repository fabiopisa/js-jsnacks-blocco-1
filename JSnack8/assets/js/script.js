var numero_utente = parseInt(prompt("digita un numero di 4 cifre"))

var number = numero_utente; 
var numeri_scomposti = [] ;
var numero_singolo = number.toString(); 

for (var i = 0, len =  numero_singolo.length; i < len; i += 1){
  numeri_scomposti.push(+ numero_singolo.charAt(i));
  console.log(numeri_scomposti);
  var somma = 

  console.log(somma)
};

